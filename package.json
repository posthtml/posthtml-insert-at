{
  "name": "posthtml-insert-at",
  "version": "0.2.2",
  "license": "MIT",
  "description": "PostHTML plugin to append or prepend HTML inside or outside of a selector",
  "author": "Eric Liu (https://github.com/metonym)",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "develop": "tsc -w",
    "build": "rm -rf lib && tsc -p tsconfig.prod.json",
    "test": "jest --coverage",
    "test:tdd": "jest --watch",
    "lint": "tslint --fix -p . -c tslint.json",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "posthtml": "^0.11.4",
    "posthtml-parser": "^0.4.1"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.0.0",
    "@commitlint/config-conventional": "^8.0.0",
    "@types/jest": "^24.0.15",
    "husky": "^3.0.0",
    "jest": "^24.8.0",
    "prettier": "^1.18.2",
    "pretty-quick": "^1.11.1",
    "ts-jest": "^24.0.2",
    "tslint": "^5.18.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.5.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn lint && pretty-quick --staged",
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "prettier": {
    "tabWidth": 2,
    "semi": true,
    "singleQuote": true
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "resolutions": {
    "**/**/lodash": "^4.17.12",
    "**/**/mixin-deep": "^2.0.1",
    "**/**/set-value": "^3.0.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/metonym/posthtml-insert-at.git"
  },
  "homepage": "https://github.com/metonym/posthtml-insert-at",
  "keywords": [
    "html",
    "posthtml",
    "posthtml-plugin"
  ],
  "files": [
    "lib/"
  ],
  "bugs": "https://github.com/metonym/posthtml-insert-at/issues"
}
